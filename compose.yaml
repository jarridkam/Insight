services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=insight_db'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=insight_user'
    ports:
      - "5433:5432"
